---
- name: Add sni proxy domains to /etc/hosts
  become: true
  lineinfile:
    dest: /etc/hosts
    regexp: '* {{ item.domain }} *'
    line: "{{ sni_server_ipv4 }} {{ item.domain }} # Auto generated by etc-hosts role"
    state: present
  with_items: "{{ sni_proxy_domains }}"
